let numeros = [2,4,5,7];

let [num, num2, num3, num4] = numeros;

console.log(num);
console.log(num2);
console.log(num3);
console.log(num4);

// Essa forma nos permite criar variáveis com os itens de um array de forma individual
/* Isso possibilita não precisarmos chamar o array e seu indice especifico, apenas chamamos a variável a qual foi atribuida o item do array */
// Funciona parecido com o destructuring de objetos